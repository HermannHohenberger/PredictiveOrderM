services:
  predictiveorder:
    container_name: predictiveorder
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-8501}:8501"
    restart: unless-stopped
